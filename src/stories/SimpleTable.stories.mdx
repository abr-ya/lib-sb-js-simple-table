import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { SimpleTable } from '../component/index';

<Meta title='SimpleTable' component={SimpleTable} />

## SimpleTable
#### A customizeable React table component

<Canvas>
  <Story name='Default'>
    <SimpleTable 
      headers={['Col 1', 'Col 2', 'Col 3', 'Col 4', 'Col 5']}
      rows={[
        ['This', 'is', 'just', 'a', 'test'],
        ['This', 'is', 'also', 'a', 'test'],
        ['Just', 'a', 'little', 'more', 'data'],
        ['Row', 'number', 'four', 'right', 'here'],
      ]}
      cellTextColor='gainsboro'
    />
  </Story>
</Canvas>
